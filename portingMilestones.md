# Rust Port Milestones

This file is the control plane for the Codex loop. The goal is to move tested, user-visible capability from OCCT-backed paths to Rust-owned paths in bounded slices.

## Working Rules

- A turn only counts as progress if it does at least one of these: deletes or narrows an OCCT fallback, expands a Rust-owned capability, or adds regression coverage for newly Rust-owned behavior.
- Treat analysis-only, probe-only, formatting-only, and helper-only turns as failed porting turns unless they are paired with a real Rust-owned behavior move in the same turn.
- Bias toward decisive, coherent cuts: replace an exercised fallback branch or capability family end-to-end instead of making the smallest local edit around it.
- Multi-file changes are expected when the port requires them. Carry a Rust-owned path through data structures, call sites, C ABI glue, integration tests, examples, and docs rather than stopping at the first seam.
- If a prerequisite refactor is needed, perform it only as part of the same turn that removes or strictly narrows an OCCT fallback or lands new tested Rust behavior.
- Use compiler and test failures as the work queue for finishing the chosen porting cut; do not retreat to a tiny safe change solely because the larger Rust replacement touches several modules.
- Stay on the exercised kernel slice first: `ported_geometry`, `brep`, `document`, `pipeline`, and their integration tests. Do not drift into placeholder `occt_port/DataExchange` files just because they are easy to touch.
- If a bounded cut stalls for one turn, record the blocker in `nextStep.md` and move to the next task inside the same milestone.

## Turn Status

- Completed evidence: `M34. Rust-Owned Root Edge Topology Bootstrap Endpoint Seed` is complete. `root_edge_topology_bootstrap_seed()` no longer calls `edge_endpoints_occt()`; it collects the root edge's endpoint vertex handles, resolves those vertices through the public Rust-owned vertex topology query, preserves closed-edge single-vertex topology, and derives the bootstrap `EdgeEndpoints` from those topology-backed vertex positions. Source guards show no `edge_endpoints_occt()` calls remain in `brep/topology.rs` or `brep/shape_queries.rs`, while `Context::edge_endpoints_occt()` remains available as an explicit oracle API.
- Active milestone: `M35. Rust-Owned Public Root Edge Support Classification`.
- Next bounded cut: remove the separate direct `edge_geometry_occt()` classifier from `ported_edge_endpoints()` by moving root-edge supported/unsupported classification into the root topology loader result, while keeping unsupported root edge kinds from falling through to the generic raw root inventory path.
- Verification:
  - `(cd rust/lean_occt && cargo fmt)`
  - `! rg -n 'edge_endpoints_occt\(' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs`
  - `rg -n 'root_edge_topology_bootstrap_seed|root_edge_vertices_from_ported_seed|root_edge_vertex_shape_occt|vertex_point\(' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs`
  - `cmake --build build --target LeanOcctCAPI`
  - `(cd rust/lean_occt && cargo check)`
  - `(cd rust/lean_occt && cargo test --test ported_geometry_workflows public_root_edge_endpoints_are_topology_backed -- --nocapture)`
  - `(cd rust/lean_occt && cargo test --test ported_geometry_workflows root_edge_endpoints_and_topology_use_ported_seed -- --nocapture)`
  - `(cd rust/lean_occt && cargo test)`
  - `git diff --check`

## M1. Rust-Owned Offset Shell Bounding Boxes

Outcome: exercised offset shells and offset solids stop depending on the final shell-local OCCT bbox fallback in `offset_shell_bbox()` in `rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/summary.rs`.

Status: complete on 2026-04-16. The exercised `brep_workflows` offset-solid fixture now records `OffsetShellBboxSource::Brep`, and the unconditional `or(Some(shell_occt_bbox))` tier has been replaced by an explicit unsupported-shell guard that only leaves OCCT as an escape hatch when no prepared shell topology inventory exists.

Definition of done: the exercised offset fixtures in `brep_workflows` stay green, the winning shell bbox path is Rust-owned for those fixtures, and the final `or(Some(shell_occt_bbox))` tier is either removed for the exercised path or isolated behind an explicit unsupported-case guard.

Bounded tasks: make the shell-bbox winner observable, promote the nearest Rust-owned winner (`validated_shell_boundary_bbox`, `validated_shell_mesh_bbox`, or `validated_shell_brep_bbox`), then delete or strictly narrow the final fallback.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`.

## M2. Whole-Shape Summary Fallback Reduction

Outcome: `ported_shape_summary()` stops using `fallback_summary()` for bbox and volume on the supported analytic, swept, and offset families already exercised in `brep_workflows`.

Status: complete on 2026-04-26. The exercised offset-solid bbox path resolves through Rust-owned shell-union data tagged as `SummaryBboxSource::OffsetSolidShellUnion`, the exercised offset-solid volume path resolves through `SummaryVolumeSource::FaceContributions` without touching the shared volume fallback, the exercised swept-revolution solid resolves its root volume through `SummaryVolumeSource::FaceContributions`, the exercised exact-primitive roots now include torus solids on `SummaryBboxSource::ExactPrimitive`, the exercised exact-curve roots stay off the generic whole-shape OCCT bbox fallback, the exercised single-face offset shell root is explicitly observed on `OffsetFaceBboxSource::ValidatedMesh`, and the exercised multi-face offset shell root resolves through `OffsetFaceBboxSource::SummaryFaceBrep`. The old whole-face `face_bboxes_occt()`/`OcctFaceUnion` bbox path and the offset-specific `offset_shape_bbox_occt()`/`OffsetOcctSubshapeUnion` path have both been removed. The remaining generic `fallback_summary()` bbox and volume branches are now behind explicit unsupported-shape guards, and fully loaded analytic, swept, or offset face inventories are promoted into Rust-owned bbox/volume requirements when existing Rust candidates cover them.

Definition of done: supported solids and shells under current tests resolve bbox and volume through Rust-owned paths plus validation, and any remaining `fallback_summary()` calls are behind explicit unsupported-shape guards instead of being the normal path.

Bounded tasks: identify each surviving `fallback_summary()` branch, replace one branch at a time starting with supported offset and swept cases, and add at least one regression that covers a family which previously fell through to OCCT.

Verification: same as M1.

## M3. Rust-Backed Traversal for Documents and Selectors

Outcome: `ModelDocument`, selectors, and high-level reports use `BrepShape` and `TopologySnapshot` for supported face and edge traversal instead of ad hoc OCCT subshape walks.

Status: complete on 2026-04-26. `ModelDocument::edges()`, `ModelDocument::faces()`, `select_edge()`, `select_face()`, and high-level inspection consume `BrepShape`, and the remaining public `Context::subshape()`/`Context::subshapes()` bridge through `shape_queries.rs` now requires `ported_topology()` before it will validate counts and return the loaded Rust topology handles. `Context::subshape_count()` also uses Rust topology for supported face, wire, edge, vertex, and shell counts, with direct OCCT count fallback kept only for unsupported shape kinds or explicit raw oracle APIs.

Definition of done: `document_workflows`, `selector_workflows`, and `recipe_workflows` stay green while the supported selector/report paths stop relying on raw `subshapes_occt()` for face and edge enumeration.

Bounded tasks: map one traversal-heavy read path at a time, switch it to `ported_brep()` or topology-backed data, then keep parity with the existing behavior through workflow tests.

Verification: `cargo test --manifest-path rust/lean_occt/Cargo.toml --test document_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test selector_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test recipe_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`.

## M4. Public Query Fallback Cleanup

Outcome: supported public geometry query APIs are served end-to-end from the ported layer for the analytic, swept, and offset kinds already covered by `ported_geometry_workflows`.

Status: complete on 2026-04-26. Public line, circle, ellipse, plane, cylinder, cone, sphere, torus, extrusion, revolution, offset payload, offset-basis payload, and swept offset-basis curve geometry/payload APIs now use Rust descriptors for matching supported kinds and return explicit Rust mismatch or unsupported-basis errors for non-matching supported descriptors. The remaining OCCT helper fallbacks in these public query APIs are isolated to `None` from the ported descriptor loaders.

Definition of done: the exercised supported query families remain green with added coverage for one newly cleaned-up public path, and OCCT helper fallbacks remain only for explicitly unsupported kinds.

Bounded tasks: remove redundant public fallbacks only after parity tests cover the same kind through the ported descriptor path, then keep unsupported cases explicit instead of implicit.

Verification: `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`.

## M5. BRep Curve Payload Fallback Cleanup

Outcome: BRep edge materialization and face topology paths stop using `PortedCurve::from_context_with_geometry()` as an OCCT-payload rescue after Rust curve-payload extraction has already identified an exercised line, circle, or ellipse edge path.

Status: complete on 2026-04-26. `PortedCurve::from_context_with_ported_payloads()` no longer calls the OCCT line, circle, or ellipse payload helpers; public BRep materialization paths in `brep_materialize.rs` and `face_topology.rs` no longer swallow extraction errors into `PortedCurve::from_context_with_geometry()`; planar root-wire reconstruction in `face_snapshot.rs` no longer rescues through the raw curve builder; `append_root_edge_sample_points()` in `swept_face.rs` uses Rust-owned ported payload extraction before falling back to OCCT point sampling only for unsupported curves; and the raw `FaceSurfaceRoute::Raw` edge path now uses the same Rust-owned curve payload extraction as the public path. `PortedCurve::from_context_with_geometry()` itself no longer calls `edge_line_payload_occt()`, `edge_circle_payload_occt()`, or `edge_ellipse_payload_occt()`.

Definition of done: exercised BRep edges with line, circle, and ellipse geometry populate `BrepEdge::ported_curve` through Rust-owned extraction, edge lengths and sampled face loops remain stable, and OCCT payload helpers are no longer the normal rescue path for supported public-route BRep edge materialization.

Bounded tasks: complete. The public-route `PortedCurve::from_context_with_ported_payloads()`, `ported_brep_edges()`, `single_face_edge_with_route()`, root-edge `face_snapshot.rs`/`swept_face.rs`, raw `FaceSurfaceRoute::Raw`, and generic `PortedCurve::from_context_with_geometry()` cuts are complete.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`.

## M6. BRep Surface Payload Fallback Cleanup

Outcome: BRep face materialization and swept/offset face descriptor paths stop using `PortedSurface::from_context_with_geometry()` as an OCCT-payload rescue after Rust surface-payload extraction has identified an exercised plane, cylinder, cone, sphere, or torus face path.

Status: complete on 2026-04-26. The BRep analytic face materialization cut is complete: `face_prepare.rs` now uses `PortedSurface::from_context_with_ported_payloads()`, and `PortedSurface::from_context_with_geometry()` no longer rescues plane, cylinder, cone, sphere, or torus payload extraction through `face_*_payload_occt()`. The analytic offset-basis cut is complete for plane, cylinder, cone, sphere, and torus. The swept offset-basis cut is complete for exercised revolution and extrusion faces: `Context::ported_offset_surface()` now builds swept basis descriptors through Rust-owned reconstructed samples and no longer retries direct `face_offset_basis_revolution_payload_occt()`, `face_offset_basis_extrusion_payload_occt()`, or `face_offset_basis_curve_*_payload_occt()` helpers for those branches. The non-offset swept BRep cut is complete for exercised extrusion and revolution faces: `brep/swept_face.rs` derives swept payloads from topology plus face samples and no longer calls `face_extrusion_payload_occt()` or `face_revolution_payload_occt()`. The planar multi-wire root-face snapshot cut is complete: `brep/face_snapshot.rs` derives the plane with `PortedSurface::from_context_with_ported_payloads()` and no longer accepts `face_plane_payload_occt()` after Rust extraction fails. The analytic face-geometry classification cut is also complete: `ported_geometry.rs::ported_face_geometry()` no longer uses direct analytic `face_*_payload_occt()` helpers as a Rust extraction rescue for plane, cylinder, cone, sphere, or torus.

Definition of done: exercised BRep faces with plane, cylinder, cone, sphere, torus, extrusion, and revolution geometry populate `BrepFace::ported_surface` or `BrepFace::ported_face_surface` through Rust-owned extraction, exercised offset descriptors build analytic and swept basis surfaces without direct `face_offset_basis_*_payload_occt()` helpers, face samples and areas remain stable, and OCCT payload helpers are no longer the normal rescue path for supported BRep face materialization or offset basis descriptor construction.

Bounded tasks: complete. Analytic BRep face materialization, analytic offset-basis descriptors, swept offset-basis descriptors, non-offset swept BRep descriptors, planar multi-wire root-face snapshot reconstruction, and analytic `ported_face_geometry()` classification no longer rescue supported surface-payload extraction through direct OCCT payload helpers.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_simple_single_face_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_face_surface_descriptors_cover_supported_faces -- --nocapture`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cmake --build build --target LeanOcctCAPI`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test document_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test selector_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `git diff --check`.

## M7. Public Payload Fallback Narrowing

Outcome: supported public payload query methods stop using direct OCCT payload helpers as the automatic rescue when Rust descriptor extraction returns `None`; the explicit `*_payload_occt()` methods remain available as opt-in parity/oracle APIs.

Status: complete on 2026-04-26. The analytic public face payload family is narrowed: `face_plane_payload()`, `face_cylinder_payload()`, `face_cone_payload()`, `face_sphere_payload()`, and `face_torus_payload()` no longer call direct `face_*_payload_occt()` helpers when Rust descriptor extraction returns `None`; they require Rust `PortedSurface` extraction for supported analytic kinds and fail explicitly if it cannot produce the payload. The swept public face payload family is narrowed: `face_revolution_payload()` and `face_extrusion_payload()` now require Rust `PortedFaceSurface::Swept` extraction for supported swept kinds and no longer rescue through direct swept OCCT payload helpers. The top-level public offset payload is narrowed: `face_offset_payload()` now requires Rust `PortedFaceSurface::Offset` extraction for supported offset faces and no longer rescues through direct `face_offset_payload_occt()`. The offset-basis face-surface family is narrowed: `face_offset_basis_geometry()` and the analytic/swept basis surface payload wrappers now require `ported_offset_face_surface_payload()` and no longer rescue through direct `face_offset_basis_*_occt()` helpers. The offset-basis curve family is narrowed: public offset-basis curve geometry and line/circle/ellipse payload wrappers now require `ported_offset_face_surface_payload()` and no longer rescue through direct `face_offset_basis_curve_*_occt()` helpers. The public edge payload family is narrowed: `edge_line_payload()`, `edge_circle_payload()`, and `edge_ellipse_payload()` now require `PortedCurve::from_context_with_ported_payloads()` and no longer rescue through direct `edge_*_payload_occt()` helpers. A full payload-fallback scan found no remaining `None => self.*payload_occt(shape)` branches in `rust/lean_occt/src/lib.rs`; tests use explicit OCCT helpers only as parity oracles.

Definition of done: for supported analytic, swept, and offset descriptors, a Rust extraction failure or kind mismatch produces an explicit Rust error/`None` distinction instead of silently returning an OCCT payload; unsupported shapes may still use explicit raw OCCT APIs only when the caller asks for them through `*_payload_occt()`.

Bounded tasks: complete. The public analytic face, swept face, offset face, offset-basis face, offset-basis curve, and edge payload families no longer silently rescue through direct OCCT payload helpers after Rust descriptor extraction returns `None`.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `! rg -n 'None => self.edge_(line|circle|ellipse)_payload_occt\\(shape\\)|edge_(line|circle|ellipse)_payload_occt\\(shape\\)' rust/lean_occt/src/lib.rs`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_analytic_curve_and_surface_payload_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_curve_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `git diff --check`.

## M8. Public Geometry and Sampling Fallback Narrowing

Outcome: supported public geometry, sampling, topology, and subshape query methods stop using direct OCCT helpers as the automatic rescue when Rust descriptors or topology can identify the shape; explicit raw OCCT methods remain available only as opt-in parity/oracle APIs or unsupported-shape escape hatches.

Status: complete on 2026-04-26. The public edge geometry/sampling family is narrowed: `edge_geometry()`, `edge_endpoints()`, `edge_sample()`, and `edge_sample_at_parameter()` now require Rust topology/curve extraction for supported line/circle/ellipse edges and only fall back to direct OCCT helpers for non-ported curve kinds. `ported_edge_geometry()` also no longer uses direct line/circle/ellipse payload helpers as a reconstruction rescue. The public face geometry/sampling family is narrowed: `face_geometry()`, `face_sample()`, and `face_sample_normalized()` now require Rust `PortedFaceSurface` extraction for supported analytic, swept, and offset faces and only fall back to direct OCCT helpers for unsupported raw surface kinds. `ported_face_geometry()` validates swept and offset descriptors before reporting ported geometry and retains Rust analytic recovery for raw BSpline/Bezier/unknown faces that sample as supported analytic surfaces. The public vertex/topology/subshape family is narrowed: `vertex_point()` and `topology()` now require Rust topology extraction, while `subshape_count()`, `subshape()`, and `subshapes()` serve `Vertex`, `Edge`, `Wire`, `Face`, and `Shell` from the loaded Rust topology inventory and fail explicitly when that inventory is unavailable.

Definition of done: for supported analytic, swept, offset, and BRep topology descriptors exercised by `ported_geometry_workflows` and `brep_workflows`, public geometry/sampling/topology queries either return Rust-owned results or fail explicitly when Rust extraction cannot cover the supported shape; they do not silently return direct OCCT helper results after descriptor extraction returns `None`.

Bounded tasks: complete. Public edge geometry/sampling, public face geometry/sampling, public vertex/topology, and public supported subshape traversal now require Rust-owned extraction before succeeding; explicit raw OCCT methods remain available as opt-in parity/oracle APIs or unsupported-kind escape hatches.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_face_free_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_simple_multi_face_solids -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_vertex_points_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test document_workflows -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test selector_workflows -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `rg -n 'rust_owned_topology_subshape_query_required|unsupported_ported_topology_query_error|ported_subshape_count' rust/lean_occt/src/lib.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs`, `git diff --check`.

## M9. BRep Materialization Fallback Narrowing

Outcome: BRep edge and face materialization stop catching Rust-owned public query failures and silently rescuing through direct OCCT geometry helpers for supported curve and surface kinds.

Status: complete on 2026-04-26. `ported_brep_edges()` in `brep_materialize.rs` no longer catches `Context::edge_geometry()` failures and retries `edge_geometry_occt()`. BRep edge materialization now uses `ported_brep_edge_geometry_and_curve()`, which requires public/Rust-owned geometry and a Rust `PortedCurve` for supported line, circle, and ellipse edges. `single_face_edge_with_route()` in `face_topology.rs` uses that same helper for both public and raw routes, so raw single-face BRep edge construction no longer has a separate direct OCCT edge-geometry bypass. `face_prepare.rs` also routes raw BRep face geometry through public `face_geometry()`, preserving explicit unsupported-kind behavior while blocking supported face materialization from bypassing the M8 Rust-owned face query guard.

Definition of done: exercised BRep edge materialization for line, circle, and ellipse edges obtains geometry and ported curves through Rust-owned extraction, unsupported curve kinds remain explicit, and BRep/document/selector workflow tests fail if a supported edge can only be materialized by direct `edge_geometry_occt()` rescue.

Bounded tasks: complete. The public and raw BRep edge materialization routes share the strict Rust-owned edge helper, the old `Err(_) => context.edge_geometry_occt(edge_shape)?` rescues are removed, and raw BRep face preparation now enters through `Context::face_geometry()` instead of direct `face_geometry_occt()`.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_exact_curve_bounding_boxes`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_area_for_offset_faces`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test document_workflows document_supports_query_driven_features`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test selector_workflows selectors_choose_expected_faces_and_edges`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `! rg -n 'Err\\(_\\) => context\\.edge_geometry_occt|FaceSurfaceRoute::Public => match context\\.edge_geometry|FaceSurfaceRoute::Raw => context\\.edge_geometry_occt|FaceSurfaceRoute::Raw => context\\.face_geometry_occt|raw_brep_edge_geometry_and_curve' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep rust/lean_occt/src/occt_port/ModelingData/TKG3d/GeomEval rust/lean_occt/src/lib.rs`, `git diff --check`.

## M10. Rust-Owned Topology Construction Geometry

Outcome: topology construction for exercised BRep roots stops using direct OCCT edge geometry and endpoint reads for supported line, circle, and ellipse edges when building root edge and wire topology.

Status: complete on 2026-04-26. `edge_topology.rs::root_edge_topology()` and `wire_topology.rs::wire_occurrence()` now use the shared `topology_edge_query()` helper, which obtains topology-construction edge geometry and endpoints through public `Context::edge_geometry()` and `Context::edge_endpoints()`. `root_wire_topology()` now attempts Rust occurrence reconstruction before the raw snapshot route, while the raw wire `topology_occt()` snapshot is kept only for duplicate-edge-occurrence wires or occurrence reconstruction misses that cannot be represented by the current unique edge shape inventory. `ported_edge_endpoints()` uses a root-edge topology seed to break the root-edge geometry/endpoints recursion without calling the direct endpoint C helper. `brep_workflows` now verifies topology edge endpoints and wire vertex chains against public edge queries and requires supported line/circle/ellipse edge coverage for exercised topology fixtures.

Definition of done: exercised root edge topology and wire occurrence matching use public/Rust-owned `edge_geometry()` and `edge_endpoints()` for supported line/circle/ellipse edges; unsupported curve kinds remain explicit; BRep topology and workflow tests fail if supported edge topology can only be reconstructed by direct raw OCCT edge geometry/endpoints.

Bounded tasks: introduce a small topology-edge query helper shared by `root_edge_topology()` and `wire_occurrence()`, route supported edge geometry/endpoints through public Rust-owned queries, keep unsupported raw OCCT reads isolated and named, and strengthen BRep workflow assertions around root edge/wire topology counts and supported edge ported curves.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_face_free_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `! rg -n 'edge_geometry_occt\\(edge_shape\\)|edge_endpoints_occt\\(edge_shape\\)|edge_geometry_occt\\(local_edge_shape\\)|edge_endpoints_occt\\(local_edge_shape\\)' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/edge_topology.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/wire_topology.rs`, `git diff --check`.

## M11. Rust-Owned Single-Face Topology Snapshot

Outcome: single-face BRep topology materialization stops using direct `context.topology_occt(face_shape)` as the normal rescue path when `context.ported_topology(face_shape)?` returns `None` for exercised supported face roots.

Status: complete on 2026-04-26. `face_topology.rs::single_face_topology_snapshot()` now requires `load_ported_topology()` and no longer has the raw `None => context.topology_occt(face_shape)?` rescue. `single_face_topology_with_route()` receives the loaded ported edge shapes instead of calling `context.subshapes_occt(face_shape, ShapeKind::Edge)`. `face_snapshot.rs::load_ported_face_snapshot()` gained Rust-owned zero-wire and single-root-wire single-face reconstruction, so boundary-free offset-extrusion and supported single-face root topology can satisfy the strict single-face loader. Regression coverage now checks single-face public ported area parity in `brep_workflows` and asserts the offset-extrusion zero-wire topology path in `ported_geometry_workflows`.

Definition of done: exercised single-face plane/cylinder/cone/sphere/torus and simple holed-planar fixtures require ported topology for `single_face_topology_snapshot()`; raw OCCT topology remains only behind an explicit unsupported or ambiguous-topology guard, and tests fail if a supported single-face BRep can only materialize through `None => context.topology_occt(face_shape)?`.

Bounded tasks: complete. The implicit raw single-face topology fallback and raw edge enumeration were removed; zero-wire and single-root-wire face snapshots now keep the exercised single-face topology family on the Rust-owned topology path.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `! rg -n 'None => context\.topology_occt\(face_shape\)\?|subshapes_occt\(face_shape, ShapeKind::Edge\)' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/face_topology.rs`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_simple_single_face_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_face_surface_descriptors_cover_supported_faces -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `git diff --check`.

## M12. Rust-Owned Planar Face Snapshot Payload

Outcome: planar face snapshot construction stops using a direct raw face-geometry read as the plane discriminator before building Rust-owned face topology fields for supported planar roots.

Status: complete on 2026-04-26. `face_snapshot.rs::ported_snapshot_plane_payload()` now calls `Context::face_geometry(face_shape)?` and then `PortedSurface::from_context_with_ported_payloads()`, so supported planar face snapshot construction enters through the public/Rust-owned face geometry gate instead of directly reading raw face geometry. The holed planar regression in `brep_workflows` now validates the plane payload through public face geometry, and the grep guard for `face_geometry_occt(face_shape)` in `face_snapshot.rs` is clean.

Definition of done: exercised planar single-wire and holed/multi-wire face snapshot paths derive their plane geometry through public/Rust-owned face geometry plus ported surface payload extraction; unsupported non-plane or ambiguous face snapshots may still return `None`, but supported planar construction fails tests if it requires direct `face_geometry_occt(face_shape)`.

Bounded tasks: complete. The plane discriminator now uses public face geometry, non-plane behavior still returns `Ok(None)`, holed planar coverage checks public plane payload extraction, and the grep guard blocks reintroducing the direct raw face snapshot call.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_simple_single_face_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_face_surface_descriptors_cover_supported_faces -- --nocapture`, `! rg -n 'face_geometry_occt\(face_shape\)' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/face_snapshot.rs`, `git diff --check`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`.

## M13. Rust-Owned Periodic Edge Geometry Direction

Outcome: public/Rust-owned circle and ellipse edge geometry no longer samples OCCT at normalized parameter `0.0` solely to determine periodic direction after Rust payload extraction has already identified the supported curve.

Status: complete on 2026-04-26. `ported_geometry.rs::ported_edge_geometry()` no longer calls `self.edge_sample_occt(shape, 0.0)?.tangent` for circle or ellipse edges. `ported_periodic_curve_geometry()` now maps endpoints onto the ported payload, evaluates candidate periodic spans against the Rust-owned curve length, and uses the already-loaded edge descriptor span only as a tie-breaker when endpoints and length are symmetric, such as closed full circles or half-period arcs. Canonicalization now preserves negative full-period spans before snapping periodic endpoints, which keeps reversed closed circles usable in BRep wire area/volume paths.

Definition of done: exercised circle and ellipse public edge geometry derives direction and parameters from Rust-owned payloads, endpoints, and edge length; public curve sampling and BRep exact-curve tests stay green; no `edge_sample_occt(shape, 0.0)?.tangent` call remains in `ported_geometry.rs`.

Bounded tasks: complete. The circle and ellipse callers no longer request raw tangent samples, the shared periodic helper handles closed full-period and partial periodic spans through endpoint/length candidate selection, the start/end parameter regression checks Rust samples against OCCT oracle samples, and the grep guard blocks reintroducing the removed raw tangent dependency.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_curve_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_face_areas_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_analytic_curve_and_surface_payload_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_exact_curve_bounding_boxes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_face_free_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `! rg -n 'edge_sample_occt\(shape, 0\.0\)\?\.tangent' rust/lean_occt/src/occt_port/ModelingData/TKG3d/GeomEval/ported_geometry.rs`, `git diff --check`.

## M14. Rust-Owned Offset Surface Descriptor Entry

Outcome: public offset surface descriptor and payload entry stop bypassing the M8 public/Rust-owned face geometry gate before reconstructing offset metadata.

Status: complete on 2026-04-26. `ported_geometry.rs::ported_offset_surface()` now starts with `self.face_geometry(shape)?` and then calls `ported_offset_surface_with_geometry()`, so the public offset descriptor/payload entry goes through the M8 public/Rust-owned face geometry gate before reconstructing offset metadata. The secondary `Context::ported_offset_face_surface_payload()` descriptor retry in `rust/lean_occt/src/lib.rs` also uses `self.face_geometry(shape)?` instead of a direct `self.face_geometry_occt(shape)?` read. `public_offset_basis_queries_match_occt` now checks public, ported, and OCCT face-geometry parity for all exercised offset basis families before public offset payload and basis queries run.

Definition of done: exercised public offset descriptor/payload queries enter through `Context::face_geometry(shape)?` before offset reconstruction, no direct `face_geometry_occt(shape)?` call remains in `ported_offset_surface()`, and offset surface sampling, offset basis queries, and BRep offset-face regressions stay green.

Bounded tasks: complete. `ported_offset_surface()` and the secondary public offset-payload descriptor retry both use public face geometry, the offset-basis workflow now exercises the public geometry gate for every supported offset basis family, and grep guards block reintroducing the removed raw geometry entry.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_swept_and_offset_payload_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_offset_basis_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_offset_surface_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_area_for_offset_faces -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `! awk '/pub\(crate\) fn ported_offset_surface/,/^    }/' rust/lean_occt/src/occt_port/ModelingData/TKG3d/GeomEval/ported_geometry.rs | rg -n 'face_geometry_occt\(shape\)'`, `! awk '/fn ported_offset_face_surface_payload/,/^    }/' rust/lean_occt/src/lib.rs | rg -n 'face_geometry_occt\(shape\)'`, `git diff --check`.

## M15. Rust-Owned Offset Surface Metadata Extraction

Outcome: exercised offset descriptor construction stops depending on direct OCCT offset metadata helpers for the offset value and basis face geometry after the public face geometry gate has identified a supported offset surface.

Status: complete on 2026-04-26. Direct analytic offset faces created through `Context::make_offset_surface_face()` now carry Rust-owned `OffsetSurfaceFaceMetadata` containing the signed offset value, basis `FaceGeometry`, and Rust analytic basis descriptor for plane, cylinder, cone, sphere, and torus bases. `ported_geometry.rs::ported_offset_surface_with_geometry()` checks that metadata before the direct OCCT offset metadata helpers, constructs `OffsetSurfacePayload` and `PortedOffsetBasisSurface::Analytic` from Rust-owned data, and validates the result against OCCT samples as an oracle only. If metadata is present but invalid, descriptor construction returns `None` instead of falling through to `face_offset_payload_occt(shape)?` or `face_offset_basis_geometry_occt(shape)?`. The analytic constructor side also requires Rust metadata extraction when OCCT identifies the basis as one of the covered analytic families.

Definition of done: for at least the exercised analytic offset-basis family, `ported_offset_surface_with_geometry()` derives `OffsetSurfacePayload { offset_value, basis_surface_kind }` and the basis `FaceGeometry` from Rust-retained construction metadata plus descriptor validation, leaving direct `face_offset_payload_occt()` and `face_offset_basis_geometry_occt()` calls only for explicit oracle APIs or unsupported-basis escape hatches. Offset basis queries, offset sampling, and BRep offset-area regressions stay green.

Bounded tasks: complete for the direct analytic family. The raw offset metadata helpers remain in `ported_offset_surface_with_geometry()` only for offset faces that have no retained Rust construction metadata, now limited to shell-derived/imported offset faces and unsupported construction paths.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_offset_basis_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_offset_surface_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_area_for_offset_faces -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `awk '/pub\(crate\) fn ported_offset_surface_with_geometry/,/let payload = self.face_offset_payload_occt/' rust/lean_occt/src/occt_port/ModelingData/TKG3d/GeomEval/ported_geometry.rs | rg -n 'offset_surface_face_metadata'`, `! awk '/fn offset_surface_face_metadata_candidate/,/^    }/' rust/lean_occt/src/lib.rs | rg -n 'face_offset_payload_occt|face_offset_basis_geometry_occt'`, `git diff --check`.

## M16. Rust-Owned Swept Offset Surface Metadata Extraction

Outcome: direct offset faces created from exercised swept extrusion and revolution bases stop depending on direct OCCT offset metadata helpers for their offset value and basis face geometry.

Status: complete on 2026-04-26. `OffsetSurfaceFaceMetadata` now carries `PortedOffsetBasisSurface`, and `Context::make_offset_surface_face()` attaches metadata for exercised analytic, extrusion, and revolution bases. Direct swept offset faces no longer reach `face_offset_payload_occt(shape)?` or `face_offset_basis_geometry_occt(shape)?` for offset value or basis face geometry; the descriptor path uses retained metadata plus Rust sample-based swept-basis reconstruction and OCCT sample validation as an oracle.

Definition of done: direct `make_offset_surface_face()` faces whose bases are exercised extrusion or revolution surfaces carry Rust-owned offset metadata with signed offset value, swept basis `FaceGeometry`, and `PortedSweptSurface` descriptor; `ported_offset_surface_with_geometry()` uses that metadata without calling `face_offset_payload_occt()` or `face_offset_basis_geometry_occt()` for direct swept offset faces; public offset payload, offset-basis swept payload/curve queries, offset sampling, and BRep offset-area regressions stay green.

Bounded tasks: complete. `OffsetSurfaceFaceMetadata` now stores analytic or swept basis descriptors, `make_offset_surface_face()` collects extrusion/revolution descriptors through the Rust swept face descriptor path, `ported_offset_surface_from_metadata()` reconstructs the swept basis from retained offset/basis metadata before the raw metadata helpers, and `public_offset_basis_queries_match_occt` proves direct swept offsets mirror source payloads and basis curve spans while matching public descriptor and OCCT oracle behavior.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_swept_and_offset_payload_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_offset_basis_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_offset_surface_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_area_for_offset_faces -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `awk '/pub\(crate\) fn ported_offset_surface_with_geometry/,/let payload = self.face_offset_payload_occt/' rust/lean_occt/src/occt_port/ModelingData/TKG3d/GeomEval/ported_geometry.rs | rg -n 'offset_surface_face_metadata'`, `! awk '/fn offset_surface_face_metadata_candidate/,/^    }/' rust/lean_occt/src/lib.rs | rg -n 'face_offset_payload_occt|face_offset_basis_geometry_occt'`, `awk '/fn offset_surface_face_metadata_supports_basis/,/^}/' rust/lean_occt/src/lib.rs | rg -n 'Revolution|Extrusion'`, `git diff --check`.

## M17. Rust-Owned Offset Metadata for Single-Face Offset Results

Outcome: exercised offset faces produced through `Context::make_offset()` from a single supported source face stop depending on direct OCCT offset metadata helpers for their offset value and basis face geometry.

Status: complete on 2026-04-26. `Context::make_offset()` now retains `ShapeRustMetadata::SingleFaceOffsetResult(...)` for supported source faces, and root topology loading reattaches it to the generated face only when the offset result contains exactly one `SurfaceKind::Offset` face. The exercised non-direct extrusion and revolution `make_offset()` faces now reach `ported_offset_surface_with_geometry()` with `OffsetSurfaceFaceMetadata`, so their offset value and basis geometry come from Rust-retained construction metadata before the raw offset metadata helpers. Regression coverage asserts source-mirroring payloads, basis geometry, basis curve spans, descriptor/public query parity, and OCCT oracle parity for those non-direct swept offset cases.

Definition of done: for exercised `make_offset(&source_face, ...)` results whose source face is a supported analytic, extrusion, or revolution face, the generated offset face receives Rust-owned metadata with signed offset value, basis `FaceGeometry`, and analytic/swept basis descriptor; `ported_offset_surface_with_geometry()` uses that metadata without the raw offset metadata helpers for those faces; public offset payload, offset-basis payload/curve queries, offset sampling, BRep offset-area, and full Rust crate tests stay green.

Bounded tasks: complete for exercised single-face offset results. The raw offset metadata helpers remain in `ported_offset_surface_with_geometry()` only for offset faces without attached Rust construction metadata, now narrowed to imported shapes, multi-face shell/solid results, unsupported construction paths, or ambiguous generated-face mappings.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_offset_basis_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_offset_surface_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_area_for_offset_faces -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_swept_and_offset_payload_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `awk '/pub\(crate\) fn ported_offset_surface_with_geometry/,/let payload = self.face_offset_payload_occt/' rust/lean_occt/src/occt_port/ModelingData/TKG3d/GeomEval/ported_geometry.rs | rg -n 'offset_surface_face_metadata'`, `! awk '/fn offset_surface_face_metadata_candidate/,/^    }/' rust/lean_occt/src/lib.rs | rg -n 'face_offset_payload_occt|face_offset_basis_geometry_occt'`, `awk '/fn attach_single_face_offset_metadata/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'single_face_offset_result_metadata|with_offset_surface_face_metadata|SurfaceKind::Offset'`, `git diff --check`.

## M18. Rust-Owned Offset Metadata for Multi-Face Offset Results

Outcome: exercised offset faces produced inside multi-face `Context::make_offset()` shell or solid results stop depending on direct OCCT offset metadata helpers once the source-to-generated face mapping is unambiguous.

Status: complete on 2026-04-26 for the exercised unambiguous multi-face offset-solid/shell family. `Context::make_offset()` now records a `MultiFaceOffsetResult` inventory by extracting Rust-owned offset metadata from supported source faces. Root topology loading validates each generated offset face against that inventory, including the signed offset variants needed by solid offsets, and attaches metadata only when exactly one source candidate reconstructs a descriptor that matches OCCT samples. The inventory is also copied onto public shell subshapes so shell-local BRep and public shell face queries keep the generated offset faces on the Rust metadata path. Ambiguous, unsupported, and imported offset faces remain on the explicit raw-helper escape hatch.

Definition of done: for at least one exercised multi-face offset shell or offset solid fixture, generated `SurfaceKind::Offset` faces are matched back to supported source face descriptors, receive per-face `OffsetSurfaceFaceMetadata`, and build public offset payload/basis descriptors without direct raw offset metadata helpers; imported, unsupported, or ambiguous multi-face results remain explicit raw-helper escape hatches.

Bounded tasks: complete for the exercised offset-solid fixture. Source-face inventory, signed variant validation, root face attachment, shell subshape metadata propagation, and public/BRep regression coverage are in place.

Verification: `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo fmt --manifest-path rust/lean_occt/Cargo.toml && cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo fmt --manifest-path rust/lean_occt/Cargo.toml && cargo check --manifest-path rust/lean_occt/Cargo.toml && cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_volume_for_offset_solids -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_area_for_offset_faces -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_offset_basis_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_offset_surface_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `! awk '/fn offset_surface_face_metadata_candidate/,/^    }/' rust/lean_occt/src/lib.rs | rg -n 'face_offset_payload_occt|face_offset_basis_geometry_occt'`, `awk '/fn attach_multi_face_offset_metadata/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'ported_offset_surface_from_metadata|with_offset_surface_face_metadata|offset_value: -candidate\.offset_value'`, `awk '/pub\(crate\) fn ported_offset_surface_with_geometry/,/let payload = self.face_offset_payload_occt/' rust/lean_occt/src/occt_port/ModelingData/TKG3d/GeomEval/ported_geometry.rs | rg -n 'offset_surface_face_metadata'`, `git diff --check`.

## M19. Rust-Owned Offset Metadata for Ambiguous Multi-Source Offset Results

Outcome: generated offset faces from multi-face `Context::make_offset()` results with multiple supported source-face candidates stop falling back to direct OCCT offset metadata helpers solely because sample validation cannot choose a unique source.

Status: complete on 2026-04-26. `attach_multi_face_offset_metadata()` now ranks validated signed source candidates instead of requiring `match_count == 1`. For every candidate that reconstructs a descriptor matching OCCT samples, it computes a Rust-owned basis-position score by backing generated offset samples onto the source basis and comparing them with the retained source `PortedOffsetSurface` descriptor sampled at zero offset. The unique best candidate is attached to the generated face through `with_offset_surface_face_metadata(...)`; invalid signed variants remain non-matches, and only no-match or indistinguishable tied-best cases keep the explicit raw-helper escape hatch. The new swept multi-source regression proves the exercised four-source offset result records four Rust source candidates, attaches Rust metadata to all four generated root offset faces and shell-local offset faces, and serves public payload, basis geometry, swept descriptors, and samples from the attached metadata before any direct raw offset metadata query can run.

Definition of done: at least one exercised multi-source offset fixture with multiple supported source candidates receives deterministic Rust-owned source/generated assignments, preserves signed offset metadata on root and shell face handles, and builds public offset payload/basis descriptors without direct raw offset metadata helpers; genuinely unsupported imported or non-ported source families remain explicit fallbacks.

Bounded tasks: complete. The existing `MultiFaceOffsetResult` inventory now carries the retained source basis descriptors needed for deterministic scoring, root and shell generated offset faces attach only the selected candidate, hidden metadata-state probes let integration coverage assert that public handles are metadata-backed before payload queries, and `ported_brep_maps_multi_source_swept_offsets_in_rust` covers the root and shell public descriptor path.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml && cargo check --manifest-path rust/lean_occt/Cargo.toml && cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_maps_multi_source_swept_offsets_in_rust -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_volume_for_offset_solids -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows public_offset_basis_queries_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_offset_surface_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `! awk '/fn offset_surface_face_metadata_candidate/,/^    }/' rust/lean_occt/src/lib.rs | rg -n 'face_offset_payload_occt|face_offset_basis_geometry_occt'`, `awk '/fn attach_multi_face_offset_metadata/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'offset_metadata_match_score|with_offset_surface_face_metadata|offset_value: -candidate\.offset_value'`, `awk '/pub\(crate\) fn ported_offset_surface_with_geometry/,/let payload = self.face_offset_payload_occt/' rust/lean_occt/src/occt_port/ModelingData/TKG3d/GeomEval/ported_geometry.rs | rg -n 'offset_surface_face_metadata'`, `git diff --check`.

## M20. Rust-Owned Wire Occurrence Topology for Repeated Edge Wires

Outcome: supported wire topology loading stops using the raw `context.topology_occt(&prepared_wire_shape.wire_shape)` snapshot fallback in `wire_topology.rs::root_wire_topology_from_snapshot()` for repeated-edge or occurrence-order cases that can be represented from Rust-owned edge occurrences.

Status: complete on 2026-04-26. The raw `root_wire_topology_from_snapshot()` function and its `context.topology_occt(&prepared_wire_shape.wire_shape)` fallback were removed. Root and face wire preparation now records ordered edge occurrence handles through the narrow C ABI `lean_occt_shape_wire_edge_occurrence_count()` and `lean_occt_shape_wire_edge_occurrence()`, while Rust maps those occurrence handles back to root edges using `lean_occt_shape_is_same()` identity plus Rust edge geometry, length, and vertex compatibility. `root_wire_topology()` now always uses the Rust occurrence path, preserving occurrence orientation and the ordered vertex chain for repeated-edge wires. The new `ported_brep_orders_repeated_wire_edge_occurrences_in_rust` regression covers the cylinder seam/repeated-edge family, asserts a wire repeats a root edge index, compares the Rust topology against the OCCT oracle, and verifies BRep materialization uses the Rust topology.

Definition of done: at least one exercised wire fixture that currently needs the snapshot fallback is represented by Rust-owned occurrence data, including ordered edge indices, edge orientations, and vertex occurrence order; `root_wire_topology_from_snapshot()` is removed or narrowed to unsupported/imported/degenerate wires; BRep topology, selector, document, and full crate regressions stay green.

Bounded tasks: complete. Ordered wire edge occurrence handles flow through root and face topology preparation, repeated edge occurrences are matched to root edge identity before the geometry fallback is considered, the full raw wire topology snapshot fallback is gone, and the cylinder repeated-edge regression plus source guard cover the moved behavior.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cmake --build build --target LeanOcctCAPI`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_orders_repeated_wire_edge_occurrences_in_rust -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test document_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test selector_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `! rg -n 'root_wire_topology_from_snapshot|topology_occt\(&prepared_wire_shape\.wire_shape\)' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/wire_topology.rs`, `git diff --check`.

## M21. Rust-Owned Root Edge Endpoint Seeds

Outcome: supported public and topology-construction edge endpoint loading stops using the raw `context.topology_occt(shape)?` snapshot in `shape_queries.rs::root_edge_endpoints_from_topology_seed()` for root edge shapes.

Status: complete on 2026-04-26. `ported_edge_endpoints()` now routes root edge shapes through `root_edge_endpoints_from_raw_endpoint_seed()` instead of the deleted full-topology seed. The seed first verifies the root kind is `ShapeKind::Edge`, admits only supported root `Line`, `Circle`, and `Ellipse` edge kinds, and then uses the existing narrow endpoint helper to recover the two endpoint coordinates needed to break root-edge topology recursion. Unsupported root edge kinds return an explicit unsupported-root-edge state, so public callers can still reach raw/oracle endpoint APIs without recursively loading ported topology. `root_edge_endpoints_and_topology_use_ported_seed` proves public, ported, OCCT oracle, and root topology endpoint parity for root line, circle, and ellipse edges.

Definition of done: supported root line, circle, and ellipse edge endpoints are seeded without `context.topology_occt(shape)?`; root edge topology and public edge endpoint/sample regressions remain green; unsupported edge kinds keep explicit oracle/raw APIs only; and source guards prevent reintroducing the old topology seed or a full topology snapshot in the new root endpoint seed.

Bounded tasks: complete. The full raw topology seed was removed, supported root line/circle/ellipse endpoint seeding now uses a narrow endpoint read, unsupported root edges avoid the recursive ported topology path, root edge public endpoint and topology parity coverage was added, and source guards block the old seed name plus any `topology_occt` call inside the new seed.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows root_edge_endpoints_and_topology_use_ported_seed -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_curve_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_face_free_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `! awk '/fn root_edge_endpoints_from_raw_endpoint_seed/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs | rg -n 'topology_occt'`, `! rg -n 'root_edge_endpoints_from_topology_seed' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs`, `git diff --check`.

## M22. Strict Supported BRep Materialization Topology Entry

Outcome: supported public BRep materialization stops using the raw `self.topology_occt(shape)?` snapshot branch in `Context::ported_brep()` when `load_ported_topology()` misses.

Status: complete on 2026-04-26. `Context::ported_brep()` now calls `strict_brep_raw_topology_fallback_allowed()` before the raw `self.topology_occt(shape)?` branch. The classifier requires ported topology for supported root line/circle/ellipse edges, face-free wires, and supported analytic/swept/offset face inventories; those roots now fail at the Rust-owned materialization boundary if `load_ported_topology()` returns `None` instead of silently materializing through `FaceSurfaceRoute::Raw`. The raw route remains available only when the classifier leaves the root in the unsupported/imported/ambiguous escape class.

Definition of done: exercised supported BRep families error at the Rust-owned boundary if `load_ported_topology()` returns `None`, raw `FaceSurfaceRoute::Raw` materialization remains only behind an explicit unsupported/imported/ambiguous-shape guard, BRep/document/selector/full crate regressions stay green, and a source guard proves `self.topology_occt(shape)?` in `ported_brep()` is no longer the unqualified fallback for supported roots.

Bounded tasks: complete. The supported-root classifier, strict `ported_brep()` gate, explicit missing-ported-topology error, representative analytic/swept/offset/face-free regression, and `ported_brep()` source guard are in place.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows supported_brep_materialization_requires_ported_topology -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_simple_multi_face_solids -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_face_free_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test document_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test selector_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `awk '/pub fn ported_brep\(&self, shape: &Shape\)/,/let vertices =/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep.rs | rg -n 'strict_brep_raw_topology_fallback_allowed|self\.topology_occt\(shape\)\?|FaceSurfaceRoute::Raw'`, `git diff --check`.

## M23. Rust-Owned Root Edge Topology Inventory

Outcome: supported root line, circle, and ellipse topology construction stops entering the generic raw root-inventory loader in `load_root_topology_snapshot()` for its edge, vertex, wire, face, and shell inventories.

Status: complete on 2026-04-26. `load_root_topology_snapshot()` now tries `load_root_edge_topology_snapshot()` before the generic raw root-inventory loader. Supported root `Line`, `Circle`, and `Ellipse` edges build topology from the Rust-owned endpoint seed, public/Rust-owned edge geometry, and Rust-owned ported curve length; the branch carries only narrow root endpoint vertex handles and a cloned root edge handle through the C ABI, with empty wire, face, and shell inventories. `root_edge_topology()` and `wire_occurrence()` share `topology_edge_length()`, so supported edge and wire topology identity uses the same ported line/circle/ellipse length source instead of OCCT linear mass-property lengths. Unsupported root edges still return `None` from the root-edge branch and keep the existing explicit raw/oracle APIs.

Definition of done: root line, circle, and ellipse `ported_topology()` and BRep materialization build their root-edge topology inventory from the Rust-owned endpoint seed and supported edge geometry/length data before the generic raw inventory path is considered; public root-edge subshape counts and topology parity stay green; unsupported root edges keep explicit raw/oracle APIs; and a source guard proves the supported root-edge branch does not call the generic `subshapes_occt()`/`vertex_point_occt()` inventory loader.

Bounded tasks: complete. The root-edge-specific topology inventory branch is installed before the generic loader, root edge vertex positions and the single topology edge are constructed from the Rust-owned endpoint seed plus ported edge geometry/length, public root-edge subshape handles/counts are served from the loaded topology inventory, regression coverage checks public vertex/edge/wire/face/shell subshapes for root edges, and source guards prove the branch does not call the generic raw inventory loader.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cmake --build build --target LeanOcctCAPI -j 8`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows root_edge_endpoints_and_topology_use_ported_seed -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_curve_sampling_matches_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_face_free_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows supported_brep_materialization_requires_ported_topology -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `awk '/fn load_root_edge_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'ported_edge_endpoints|topology_edge_length|duplicate_shape_occt|root_edge_vertex_shape_occt'`, `! awk '/fn load_root_edge_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'subshapes_occt|vertex_point_occt|topology_occt'`, `awk '/pub\(super\) fn topology_edge_length/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/edge_topology.rs | rg -n 'PortedCurve::from_context_with_ported_payloads|edge_length\(edge_shape\)|Rust-owned topology did not cover'`, `awk '/fn wire_occurrence/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/wire_topology.rs | rg -n 'topology_edge_length|edge_length\(edge_shape\)'`, `git diff --check`.

## M24. Rust-Owned Face-Free Root Wire Inventory

Outcome: face-free root wire topology construction stops entering the generic raw root-inventory loader in `load_root_topology_snapshot()` for its vertex, edge, wire, face, and shell inventories when the wire can be represented by Rust-owned edge occurrence data.

Status: complete on 2026-04-26. `load_root_topology_snapshot()` now routes face-free root wires through `load_root_wire_topology_snapshot()` after the M23 root-edge branch and before the generic raw inventory loader. The root-wire branch clones only the root wire handle, gets ordered edge occurrence handles through `wire_edge_occurrences_occt()`, derives edge geometry/endpoints through `topology_edge_query()`, derives edge lengths through `topology_edge_length()`, deduplicates vertex handles/positions from the occurrence endpoint seed, and packs the single root wire through the existing occurrence-ordering path. If a face-free root wire cannot be represented unambiguously by those occurrence handles, the branch returns `None` instead of falling through to the generic raw inventory sweep.

Definition of done: exercised face-free root wires build their root wire handle, edge inventory, vertex positions, packed wire topology, public subshape counts/handles, and BRep materialization from narrow wire occurrence handles plus Rust-owned edge geometry/endpoints/length before the generic raw inventory loader is considered; unsupported or ambiguous root wires keep explicit raw/oracle escapes; and a source guard proves the named root-wire branch avoids `subshapes_occt(shape, Vertex/Edge/Wire/Face/Shell)` and `vertex_point_occt()`.

Bounded tasks: complete. The root-wire-specific topology inventory branch is installed before the generic loader but after the root-edge branch, the exercised helix root wire is seeded from a cloned root handle plus `wire_edge_occurrences_occt()`, edge and vertex topology are derived from occurrence handles using `topology_edge_query()` and `topology_edge_length()`, vertices are deduplicated by endpoint identity/coordinates, the single root wire is packed with existing occurrence ordering, the face-free helix regression checks public wire/edge/vertex handles and lengths, and source guards prove the named branch avoids the generic raw inventory calls.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_face_free_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows root_edge_endpoints_and_topology_use_ported_seed -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows supported_brep_materialization_requires_ported_topology -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `awk '/fn load_root_wire_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'wire_edge_occurrences_occt|topology_edge_query|topology_edge_length|duplicate_shape_occt'`, `awk '/fn root_wire_vertex_index_from_ported_seed/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_edge_vertex_shape_occt|shape_is_same_occt|match_vertex_index'`, `! awk '/fn load_root_wire_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'subshapes_occt|vertex_point_occt|topology_occt'`, `awk '/fn load_root_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'load_root_edge_topology_snapshot|root_wire_topology_inventory_required|load_root_wire_topology_snapshot|subshapes_occt'`, `git diff --check`. No C ABI glue changed in this milestone.

## M25. Rust-Owned Root Vertex Topology Inventory

Outcome: root vertex topology construction stops entering the generic raw root-inventory loader in `load_root_topology_snapshot()` for its vertex, edge, wire, face, and shell inventories.

Status: complete on 2026-04-26. `load_root_topology_snapshot()` now routes root vertices through `load_root_vertex_topology_snapshot()` after the M23 root-edge branch and before the M24 root-wire branch and generic raw inventory loader. The root-vertex branch clones only the root vertex handle, reads the point through the narrow C ABI `lean_occt_shape_root_vertex_point()` wrapped by `root_vertex_point_seed_occt()`, returns a one-position topology snapshot with empty edge, wire, face, and shell inventories, and returns `None` for malformed root vertices instead of falling through to the generic root inventory sweep. `ported_vertex_points_match_occt` now covers public topology, public vertex subshape count/handles, public indexed vertex handle, public vertex point, and empty edge/wire/face/shell inventories for exercised root vertices.

Definition of done: root vertex `ported_topology()`, public `vertex_point()`, public subshape counts/handles, and any BRep/report consumers build the one-vertex topology from a root-vertex-specific branch before the generic raw inventory path is considered; unsupported or malformed vertex roots keep explicit raw/oracle APIs; and a source guard proves the root-vertex branch avoids the generic `subshapes_occt()` inventory sweep.

Bounded tasks: complete. The root-vertex-specific topology inventory branch is installed before the generic loader, root vertex handles and positions are seeded from a cloned root handle plus `root_vertex_point_seed_occt()`, the public topology/subshape APIs consume the one-vertex loaded inventory, the vertex workflow regression covers the moved behavior, and source guards prove the branch avoids the generic raw inventory calls.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cmake --build build --target LeanOcctCAPI -j 8`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_vertex_points_match_occt -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows root_edge_endpoints_and_topology_use_ported_seed -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_face_free_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `awk '/fn load_root_vertex_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_vertex_point_seed_occt|duplicate_shape_occt'`, `! awk '/fn load_root_vertex_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'subshapes_occt|vertex_point_occt|topology_occt'`, `awk '/fn load_root_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'load_root_edge_topology_snapshot|root_vertex_topology_inventory_required|load_root_vertex_topology_snapshot|root_wire_topology_inventory_required|load_root_wire_topology_snapshot|subshapes_occt'`, `rg -n 'lean_occt_shape_root_vertex_point|root_vertex_point_seed_occt' capi/include/lean_occt_capi.h capi/src/lean_occt_capi.cxx rust/lean_occt/src/lib.rs`, `git diff --check`.

## M26. Rust-Owned Root Face Topology Inventory

Outcome: supported single root-face topology construction stops entering the generic raw root-inventory loader in `load_root_topology_snapshot()` for its vertex, edge, wire, face, and shell inventories.

Status: complete on 2026-04-26. `load_root_topology_snapshot()` now routes supported single root faces through `load_root_face_topology_snapshot()` after the completed root edge, root vertex, and face-free root-wire branches and before the generic raw inventory loader. The root-face branch clones the root face handle, reads ordered face-local wire, edge, and vertex handles through narrow C ABI seeds, derives vertex positions through the root vertex point seed, builds root edge topology from Rust-owned edge geometry/endpoints/length, packs face wires through the existing ordered wire-occurrence path, and prepares the single face without calling the root-level generic inventory loader. If the root face cannot satisfy the supported single-face guard, the branch returns `None` and leaves unsupported or ambiguous roots on the explicit raw/oracle APIs.

Definition of done: supported root `Face` `ported_topology()`, public face/wire/edge/vertex subshape counts and handles, public BRep materialization, and face report consumers build the single-face topology from a root-face-specific branch before the generic raw root inventory path is considered; unsupported or ambiguous root faces keep explicit raw/oracle APIs; and a source guard proves the root-face branch avoids the root-level generic `subshapes_occt(shape, Vertex/Edge/Wire/Face/Shell)` sweep.

Bounded tasks: complete. Narrow face-local wire, edge, and vertex C ABI seeds plus an edge-local vertex-inventory seed now feed crate-private Rust wrappers; `load_root_face_topology_snapshot()` is installed before the generic loader but after the completed root edge/vertex/wire branches; the branch is guarded to supported `ShapeKind::Face` roots with one face and no shell; unique edge and vertex inventories are built through ordered face seeds, ordered wire occurrences, and Rust-owned edge topology; the simple single-face BRep/topology regression checks public root-face subshape handles, topology handles, edge lengths, vertex points, and empty shell inventory; and source guards prove the branch avoids `subshapes_occt()`, `vertex_point_occt()`, and `topology_occt()`.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cmake --build build --target LeanOcctCAPI -j 8`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_simple_single_face_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows ported_face_surface_descriptors_cover_supported_faces -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_face_free_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `awk '/fn load_root_face_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_face_(wire|edge|vertex)_shapes_occt|wire_edge_occurrences_occt|root_edge_topology|root_vertex_point_seed_occt|duplicate_shape_occt|attach_offset_result_face_metadata'`, `! awk '/fn load_root_face_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'subshapes_occt|vertex_point_occt|topology_occt'`, `awk '/fn load_root_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'load_root_edge_topology_snapshot|root_vertex_topology_inventory_required|load_root_vertex_topology_snapshot|root_wire_topology_inventory_required|load_root_wire_topology_snapshot|root_face_topology_inventory_required|load_root_face_topology_snapshot|subshapes_occt'`, `rg -n 'lean_occt_shape_root_face_(wire|edge|vertex)|root_face_(wire|edge|vertex)_shapes_occt|lean_occt_shape_edge_vertex_inventory|edge_vertex_inventory_shape_occt' capi/include/lean_occt_capi.h capi/src/lean_occt_capi.cxx rust/lean_occt/src/lib.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs`, `git diff --check`.

## M27. Rust-Owned Root Shell Topology Inventory

Outcome: supported root-shell topology construction stops entering the generic raw root-inventory loader in `load_root_topology_snapshot()` for its vertex, edge, wire, face, and shell inventories.

Status: complete on 2026-04-26. `load_root_topology_snapshot()` now routes supported root shells through `load_root_shell_topology_snapshot()` after the completed root edge, root vertex, face-free root-wire, and root-face branches and before the generic raw inventory loader. Narrow shell-local face, wire, edge, and vertex C ABI seeds feed crate-private Rust wrappers; the branch clones the root shell handle, reattaches retained offset metadata, derives vertex positions through `root_vertex_point_seed_occt()`, builds root edge topology from Rust-owned edge geometry/endpoints/length, packs shell-local wires through ordered wire occurrences, prepares each supported face through the shared root-face machinery, and prepares the single shell from those face handles. If a guarded root shell cannot be represented through those seeds, the branch returns `None` instead of entering the generic raw root inventory sweep.

Definition of done: supported root `Shell` `ported_topology()`, public shell/face/wire/edge/vertex subshape counts and handles, public BRep materialization, and shell report consumers build shell topology from a root-shell-specific branch before the generic raw root inventory path is considered; unsupported or ambiguous root shells, solids, and compounds keep explicit raw/oracle APIs; and source guards prove the root-shell branch avoids the root-level generic `subshapes_occt(shape, Vertex/Edge/Wire/Face/Shell)` sweep.

Bounded tasks: complete. The root-shell-specific topology inventory branch is installed before the generic loader but after the completed root edge/vertex/wire/face branches; it is guarded to supported `ShapeKind::Shell` roots with one shell and no solid/compound inventory; shell-local face/wire/edge/vertex seeds assemble unique topology inventories without the root-level generic sweep; the shared root-face preparation helpers now serve both root faces and root shell faces; the multi-face solid regression now pulls each solid-derived shell as a root shell and checks public shell/face/wire/edge/vertex handles, isolated topology handles, edge lengths, vertex points, and BRep topology parity; and source guards prove the branch avoids `subshapes_occt()`, `vertex_point_occt()`, and `topology_occt()`.

Verification: `cargo fmt --manifest-path rust/lean_occt/Cargo.toml`, `cmake --build build --target LeanOcctCAPI -j 8`, `cargo check --manifest-path rust/lean_occt/Cargo.toml`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_simple_multi_face_solids -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows ported_brep_uses_rust_owned_topology_for_simple_single_face_shapes -- --nocapture`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test brep_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml --test ported_geometry_workflows`, `cargo test --manifest-path rust/lean_occt/Cargo.toml`, `awk '/fn load_root_shell_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_shell_(face|wire|edge|vertex)_shapes_occt|root_face_wire_shapes_occt|wire_edge_occurrences_occt|root_edge_topology|root_vertex_point_seed_occt|duplicate_shape_occt|attach_offset_result_face_metadata'`, `! awk '/fn load_root_shell_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'subshapes_occt|vertex_point_occt|topology_occt'`, `awk '/fn prepare_root_face_wire_shapes/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_face_wire_shapes_occt|prepare_root_wire_shape'`, `awk '/fn prepare_root_wire_shape/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'wire_edge_occurrences_occt'`, `awk '/fn load_root_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'load_root_edge_topology_snapshot|root_vertex_topology_inventory_required|load_root_vertex_topology_snapshot|root_wire_topology_inventory_required|load_root_wire_topology_snapshot|root_face_topology_inventory_required|load_root_face_topology_snapshot|root_shell_topology_inventory_required|load_root_shell_topology_snapshot|subshapes_occt'`, `rg -n 'lean_occt_shape_root_shell_(face|wire|edge|vertex)|root_shell_(face|wire|edge|vertex)_shapes_occt|requireShellShape' capi/include/lean_occt_capi.h capi/src/lean_occt_capi.cxx rust/lean_occt/src/lib.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs`, `git diff --check`.

## M28. Rust-Owned Root Solid Topology Inventory

Outcome: supported root-solid topology construction stops entering the generic raw root-inventory loader in `load_root_topology_snapshot()` for its vertex, edge, wire, face, and shell inventories.

Status: complete on 2026-04-26. `load_root_topology_snapshot()` now routes supported root solids through `load_root_solid_topology_snapshot()` after the completed root edge, root vertex, face-free root-wire, root-face, and root-shell branches and before the generic raw inventory loader. Narrow solid-local shell, face, wire, edge, and vertex C ABI seeds feed crate-private Rust wrappers; the branch clones the root solid handle, derives solid-wide vertex positions through `root_vertex_point_seed_occt()`, builds root edge topology from Rust-owned edge geometry/endpoints/length, packs solid-local ordered wire occurrences, prepares each solid face through the shared root-face machinery, and prepares each solid shell with shell-local face/edge/vertex inventories. If a guarded root solid cannot be represented through those seeds, the branch returns `None` instead of entering the generic raw root inventory sweep.

Definition of done: supported root `Solid` `ported_topology()`, public shell/face/wire/edge/vertex subshape counts and handles, public BRep materialization, summary/report consumers, and solid-derived shell ranges build topology from a root-solid-specific branch before the generic raw root inventory path is considered; compounds, compsolids, and ambiguous non-solid assemblies keep explicit raw/oracle APIs; and source guards prove the root-solid branch avoids the root-level generic `subshapes_occt(shape, Vertex/Edge/Wire/Face/Shell)` sweep.

Bounded tasks: complete. The root-solid-specific topology inventory branch is installed before the generic loader but after the completed root edge/vertex/wire/face/shell branches; it is guarded to supported `ShapeKind::Solid` roots with one solid, one or more shells, and no compound/compsolid inventory; solid-local shell/face/wire/edge/vertex seeds assemble unique topology inventories without the root-level generic sweep; the shared root-face and root-wire preparation helpers now serve root solids; the multi-face solid regression now checks the root solid itself through public shell/face/wire/edge/vertex handles, isolated shell/face/wire topology handles, edge lengths, vertex points, and BRep topology parity; and source guards prove the branch avoids `subshapes_occt()`, `vertex_point_occt()`, and `topology_occt()`.

Verification: `(cd rust/lean_occt && cargo fmt)`, `cmake --build build --target LeanOcctCAPI`, `(cd rust/lean_occt && cargo check)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_simple_multi_face_solids --test brep_workflows -- --nocapture)`, `(cd rust/lean_occt && cargo test --test brep_workflows -- --nocapture)`, `(cd rust/lean_occt && cargo test --lib)`, `(cd rust/lean_occt && cargo test)`, `awk '/fn load_root_solid_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_solid_(shell|face|wire|edge|vertex)_shapes_occt|root_shell_(face|edge|vertex)_shapes_occt|prepare_root_face_wire_shapes|prepare_root_wire_shape|root_edge_topology|root_vertex_point_seed_occt|duplicate_shape_occt|attach_offset_result_face_metadata'`, `! awk '/fn load_root_solid_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'subshapes_occt|vertex_point_occt|topology_occt'`, `awk '/fn prepare_root_face_wire_shapes/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_face_wire_shapes_occt|prepare_root_wire_shape'`, `awk '/fn prepare_root_wire_shape/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'wire_edge_occurrences_occt'`, `awk '/fn load_root_topology_snapshot/,/^}/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'load_root_edge_topology_snapshot|root_vertex_topology_inventory_required|load_root_vertex_topology_snapshot|root_wire_topology_inventory_required|load_root_wire_topology_snapshot|root_face_topology_inventory_required|load_root_face_topology_snapshot|root_shell_topology_inventory_required|load_root_shell_topology_snapshot|root_solid_topology_inventory_required|load_root_solid_topology_snapshot|subshapes_occt'`, `rg -n 'lean_occt_shape_root_solid_(shell|face|wire|edge|vertex)|root_solid_(shell|face|wire|edge|vertex)_shapes_occt|requireSolidShape' capi/include/lean_occt_capi.h capi/src/lean_occt_capi.cxx rust/lean_occt/src/lib.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs`, `git diff --check`.

## M29. Rust-Owned Root Compound and CompSolid Topology Inventory

Outcome: supported root compound/compsolid topology construction stops entering the generic raw root-inventory loader in `load_root_topology_snapshot()` for its vertex, edge, wire, face, shell, and solid inventories.

Status: complete on 2026-04-26. The exercised direct-solid root `Compound`, direct-shell root `Compound`, and direct-solid root `CompSolid` cases now bypass the generic raw root inventory path through `load_root_assembly_topology_snapshot()`. Root-assembly-specific typed C ABI seeds expose root-wide subshape inventories, direct-child C ABI seeds classify immediate root assembly children before the branch is accepted, and `lean_occt_shape_make_compsolid()` plus `ModelKernel::make_compsolid()` provide an exercised `CompSolid` fixture. Public solid and shell handles for loaded assembly roots are Rust-owned, multi-solid assembly volume sums loaded child solid summaries instead of falling back to signed root face cancellation, and unsupported root assemblies stop before the generic raw root inventory sweep instead of using it as an implicit fallback.

Definition of done: a supported root `Compound` or `CompSolid` whose direct children are already-supported solids or shells builds `ported_topology()`, public subshape counts/handles, BRep materialization, summary/report consumers, and child shell/solid ranges from a root-assembly-specific branch before the generic raw root inventory path is considered; nested/unsupported/imported assemblies keep explicit raw/oracle APIs; and source guards prove the root-assembly branch avoids the root-level generic inventory sweep.

Bounded tasks: complete. The root-assembly branch is installed after the completed root-solid branch and before the generic raw root inventory sweep; it accepts only direct root `Compound` children that are supported solids or shells and direct root `CompSolid` children that are supported solids; it prepares root-wide topology through the shared root wire/face/shell machinery; it carries child solid/shell handles into public queries and summaries; and it returns `None` for unsupported assembly roots so they do not reach the generic raw root inventory path.

Verification: `(cd rust/lean_occt && cargo fmt)`, `cmake --build build --target LeanOcctCAPI`, `(cd rust/lean_occt && cargo check)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_root_compound_solids -- --nocapture)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_root_compsolid_solids -- --nocapture)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_root_compound_shells -- --nocapture)`, `(cd rust/lean_occt && cargo test)`, `perl -0ne 'print $1 if /(fn load_root_assembly_topology_snapshot[\s\S]*?)\nfn root_assembly_subshapes_occt/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_assembly_subshapes_occt|root_shell_(face|edge|vertex)_shapes_occt|prepare_root_face_wire_shapes|prepare_root_wire_shape|root_edge_topology|root_vertex_point_seed_occt|duplicate_shape_occt|attach_offset_result_face_metadata'`, `! perl -0ne 'print $1 if /(fn load_root_assembly_topology_snapshot[\s\S]*?)\nfn root_assembly_subshapes_occt/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n '\bsubshapes_occt\(|\bvertex_point_occt\(|\btopology_occt\('`, `perl -0ne 'print $1 if /(fn root_assembly_topology_inventory_required[\s\S]*?)\nfn load_root_assembly_topology_snapshot/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'RootAssemblyTopologyInventory::(Supported|Unsupported)|root_(compound|compsolid)_child_shapes_occt|ShapeKind::(Solid|Shell)'`, `awk '/match root_assembly_topology_inventory_required/,/let vertex_shapes/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'Supported\\(|Unsupported => return Ok\\(None\\)'`, `rg -n 'lean_occt_shape_make_compsolid|lean_occt_shape_root_(compound|compsolid)_(child|subshape)|root_(compound|compsolid)_(child_shapes|subshapes)_occt|RootAssemblyTopologyInventory' capi/include/lean_occt_capi.h capi/src/lean_occt_capi.cxx rust/lean_occt/src/lib.rs rust/lean_occt/src/high_level.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs`, `git diff --check`.

## M30. Rust-Owned Nested Assembly Topology Inventory

Outcome: supported nested root assembly topology construction stops treating direct child `Compound`/`CompSolid` nodes as unsupported when every descendant root is already representable by the M29 direct-child solid/shell assembly branch.

Status: complete on 2026-04-26. Supported nested root `Compound` assemblies now bypass the generic raw root inventory path when descendant leaves are supported solids or shells. `lean_occt_shape_make_compound()` and `ModelKernel::make_compound()` provide deterministic nested assembly fixtures; the direct-child assembly classifier now recurses through child `Compound` and `CompSolid` nodes with a depth guard; and unsupported descendants still return `RootAssemblyTopologyInventory::Unsupported` so the unsupported assembly stop remains explicit. The exercised nested `Compound -> Compound -> solids`, `Compound -> CompSolid -> solids`, and `Compound -> Compound -> shells` cases build root-wide topology through the root-assembly branch, verify direct child shape kinds, public solid/shell handles, child topology, BRep topology, edge geometry/length parity, and summary parity.

Definition of done: an exercised nested root `Compound` or `CompSolid` with supported descendant solids/shells builds `ported_topology()`, public subshape counts/handles, BRep materialization, summary/report consumers, and child ranges through Rust-owned recursive/direct-child assembly inventory; unrelated imported or unsupported assemblies remain explicit raw/oracle-only paths; and source guards prove nested assembly support does not re-open the generic raw root inventory sweep.

Bounded tasks: complete. Deterministic nested root `Compound` fixtures are available through the new compound constructor; direct-child wrappers are public for parity assertions; recursive supported-child classification admits nested `Compound` and `CompSolid` nodes without reopening the generic raw sweep; unsupported child kinds still return `RootAssemblyTopologyInventory::Unsupported`; and workflow/source coverage locks the nested solid/compsolid/shell cases to the root-assembly branch.

Verification: `(cd rust/lean_occt && cargo fmt)`, `cmake --build build --target LeanOcctCAPI`, `(cd rust/lean_occt && cargo check)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_nested_root_compound_solids -- --nocapture)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_nested_root_compound_compsolid -- --nocapture)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_nested_root_compound_shells -- --nocapture)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_root_compound_shells -- --nocapture)`, `(cd rust/lean_occt && cargo test)`, `perl -0ne 'print $1 if /(fn load_root_assembly_topology_snapshot[\s\S]*?)\nfn root_assembly_subshapes_occt/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_assembly_subshapes_occt|root_shell_(face|edge|vertex)_shapes_occt|prepare_root_face_wire_shapes|prepare_root_wire_shape|root_edge_topology|root_vertex_point_seed_occt|duplicate_shape_occt|attach_offset_result_face_metadata'`, `! perl -0ne 'print $1 if /(fn load_root_assembly_topology_snapshot[\s\S]*?)\nfn root_assembly_subshapes_occt/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n '\bsubshapes_occt\(|\bvertex_point_occt\(|\btopology_occt\('`, `perl -0ne 'print $1 if /(fn root_assembly_topology_inventory_required[\s\S]*?)\nfn load_root_assembly_topology_snapshot/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'root_assembly_child_topology_supported|root_compound_topology_inventory_required\(context, child_shape, depth \+ 1\)|root_compsolid_topology_inventory_required\(context, child_shape, depth \+ 1\)|ShapeKind::(Compound|CompSolid|Solid|Shell)|RootAssemblyTopologyInventory::Unsupported|ROOT_ASSEMBLY_MAX_DEPTH'`, `awk '/match root_assembly_topology_inventory_required/,/let vertex_shapes/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'Supported\\(|Unsupported => return Ok\\(None\\)'`, `rg -n 'lean_occt_shape_make_compound|lean_occt_shape_make_compsolid|make_compound|make_compsolid|root_compound_child_shapes_occt|root_compsolid_child_shapes_occt|ROOT_ASSEMBLY_MAX_DEPTH|root_assembly_child_topology_supported|ported_brep_uses_rust_owned_topology_for_nested_root_compound_(solids|compsolid|shells)' capi/include/lean_occt_capi.h capi/src/lean_occt_capi.cxx rust/lean_occt/src/lib.rs rust/lean_occt/src/high_level.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs rust/lean_occt/tests/brep_workflows.rs`, `perl -0ne 'print $1 if /(fn ported_brep_uses_rust_owned_topology_for_nested_root_compound_solids[\s\S]*?)\n\#\[test\]\nfn ported_brep_uses_rust_owned_topology_for_nested_root_compound_compsolid/' rust/lean_occt/tests/brep_workflows.rs | rg -n 'assert_root_compound_child_kinds|ShapeKind::Compound|ShapeKind::Solid'`, `perl -0ne 'print $1 if /(fn ported_brep_uses_rust_owned_topology_for_nested_root_compound_compsolid[\s\S]*?)\nfn assert_ported_root_assembly_shells/' rust/lean_occt/tests/brep_workflows.rs | rg -n 'assert_root_compound_child_kinds|root_compsolid_child_shapes_occt|ShapeKind::CompSolid|ShapeKind::Solid'`, `perl -0ne 'print $1 if /(fn ported_brep_uses_rust_owned_topology_for_nested_root_compound_shells[\s\S]*?)\n\#\[test\]\nfn ported_brep_summarizes/' rust/lean_occt/tests/brep_workflows.rs | rg -n 'assert_root_compound_child_kinds|ShapeKind::Compound|ShapeKind::Shell'`, `git diff --check`.

## M31. Rust-Owned Face-Free Compound Assembly Topology Inventory

Outcome: supported root `Compound` assemblies whose direct or nested leaves are existing Rust-owned `Face`, face-free `Wire`, `Edge`, or `Vertex` roots stop entering the generic raw root inventory loader solely because the assembly branch currently assumes shell inventory.

Status: complete on 2026-04-26. Supported shell-free root `Compound` assemblies now enter the recursive assembly branch when their direct or nested leaves are existing Rust-owned `Face`, face-free `Wire`, `Edge`, or `Vertex` roots. `root_assembly_child_topology_supported()` accepts those leaf roots only after their existing root-specific topology guards/loaders succeed, `root_assembly_topology_inventory_required()` no longer rejects every `Compound` with zero faces or zero shells before child classification, and `load_root_assembly_topology_snapshot()` no longer returns `None` solely because face or shell inventories are empty. Unsupported child roots still return `RootAssemblyTopologyInventory::Unsupported`, and the assembly loader still avoids raw root-level `subshapes_occt()`, `vertex_point_occt()`, and `topology_occt()` calls.

Definition of done: exercised root `Compound` assemblies made from supported face and face-free wire/edge/vertex leaves build `ported_topology()`, public subshape counts/handles, BRep materialization, summary/report consumers, and leaf topology through Rust-owned recursive assembly inventory; unsupported child kinds still stop before the generic raw sweep; and source guards prove the loader no longer requires shell inventory for the exercised face-free assembly family.

Bounded tasks: complete. Deterministic compound workflow fixtures cover supported face leaves, nested face-free wire leaves, direct edge leaves, and direct vertex leaves; the recursive assembly classifier admits those leaf roots only through the completed root face/wire/edge/vertex loaders; shell-free assemblies now populate face/wire/edge/vertex inventories while returning empty shell/solid inventories; workflow coverage checks public leaf handles, BRep topology, summary area/length parity, child topology, and direct child kinds; and source guards prove face-free compounds enter the root-assembly branch without reintroducing root-level generic `subshapes_occt()`, `vertex_point_occt()`, or `topology_occt()`.

Verification: `(cd rust/lean_occt && cargo fmt)`, `cmake --build build --target LeanOcctCAPI`, `(cd rust/lean_occt && cargo check)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_root_compound_faces -- --nocapture)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_nested_root_compound_face_free_wires -- --nocapture)`, `(cd rust/lean_occt && cargo test ported_brep_uses_rust_owned_topology_for_root_compound_edges_and_vertices -- --nocapture)`, `(cd rust/lean_occt && cargo test)`, `perl -0ne 'print $1 if /(fn root_assembly_topology_inventory_required[\s\S]*?)\nfn load_root_assembly_topology_snapshot/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'ShapeKind::(Face|Wire|Edge|Vertex)|load_root_(face|wire|edge|vertex)_topology_snapshot|root_(face|wire|vertex)_topology_inventory_required|root_assembly_child_topology_supported|RootAssemblyTopologyInventory::Unsupported'`, `! perl -0ne 'print $1 if /(fn root_assembly_topology_inventory_required[\s\S]*?)\nfn root_compound_topology_inventory_required/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'face_count == 0 \|\| summary\.shell_count == 0'`, `! perl -0ne 'print $1 if /(fn load_root_assembly_topology_snapshot[\s\S]*?)\nfn root_assembly_subshapes_occt/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n 'face_shapes\.is_empty\(\)|root_assembly_shell_shapes\.is_empty\(\)'`, `! perl -0ne 'print $1 if /(fn load_root_assembly_topology_snapshot[\s\S]*?)\nfn root_assembly_subshapes_occt/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs | rg -n '\bsubshapes_occt\(|\bvertex_point_occt\(|\btopology_occt\('`, `rg -n 'ported_brep_uses_rust_owned_topology_for_root_compound_(faces|edges_and_vertices)|ported_brep_uses_rust_owned_topology_for_nested_root_compound_face_free_wires|ShapeKind::(Face|Wire|Edge|Vertex)|load_root_(face|wire|edge|vertex)_topology_snapshot' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs rust/lean_occt/tests/brep_workflows.rs`, `git diff --check`.

## M32. Strict Rust-Owned BRep Requirement for Supported Face-Free Assemblies

Outcome: `Context::ported_brep()` stops permitting the raw `topology_occt()`/`subshapes_occt()` BRep fallback for supported root `Compound` assemblies just because `face_count == 0`; supported recursive assembly roots must either load Rust topology or fail explicitly.

Status: complete on 2026-04-26. `strict_brep_requires_ported_topology()` now handles root `Compound` assemblies before the generic zero-face exit. It reuses the root-assembly classifier through `root_assembly_requires_ported_topology()`, requires ported topology for supported direct or nested face-free assemblies, and only returns `Ok(false)` for zero-face compounds after the assembly classifier reports unsupported or non-assembly. This closes the raw BRep fallback for the M31 wire/edge/vertex compound families without forcing unsupported/imported assemblies into the strict path.

Definition of done: supported direct/nested face-free `Compound` assemblies are included in the strict BRep requirement before raw topology fallback is allowed; unsupported/imported assemblies remain explicit raw/oracle-only paths; workflow tests cover BRep materialization for wire/edge/vertex compounds; and source guards prove the strict branch no longer short-circuits all `Compound` roots on `face_count == 0`.

Bounded tasks: complete. The topology module exposes a crate-local `root_assembly_requires_ported_topology()` classifier; the strict BRep gate checks supported compound assemblies before testing `summary.face_count == 0`; unsupported/non-assembly compounds keep their raw/oracle route; crate coverage asserts strict requirements for nested wire compounds, edge compounds, vertex compounds, and mixed wire/edge/vertex compounds; and focused workflow/source guards lock the existing M31 assembly materialization path.

Verification: `(cd rust/lean_occt && cargo fmt)`, `cmake --build build --target LeanOcctCAPI`, `(cd rust/lean_occt && cargo check)`, `(cd rust/lean_occt && cargo test strict_brep_requires_ported_topology_for_supported_face_free_compounds --lib -- --nocapture)`, `(cd rust/lean_occt && cargo test --test brep_workflows ported_brep_uses_rust_owned_topology_for_root_compound_faces -- --nocapture)`, `(cd rust/lean_occt && cargo test --test brep_workflows ported_brep_uses_rust_owned_topology_for_nested_root_compound_face_free_wires -- --nocapture)`, `(cd rust/lean_occt && cargo test --test brep_workflows ported_brep_uses_rust_owned_topology_for_root_compound_edges_and_vertices -- --nocapture)`, `perl -0ne 'exit(/ShapeKind::Compound => \{\s*if root_assembly_requires_ported_topology\(context, shape\)\? \{[\s\S]*?\} else if summary\.face_count == 0/ ? 0 : 1)' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep.rs`, `perl -0ne 'print $1 if /(fn strict_brep_requires_ported_topology[\s\S]*?)\nfn strict_brep_root_edge_requires_ported_topology/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep.rs | rg -n 'ShapeKind::Compound|root_assembly_requires_ported_topology|summary\.face_count == 0|strict_brep_face_inventory_requires_ported_topology'`, `rg -n 'pub\(super\) fn root_assembly_requires_ported_topology|RootAssemblyTopologyInventory::Supported|root_assembly_topology_inventory_required' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs`, `rg -n 'strict_brep_requires_ported_topology_for_supported_face_free_compounds|mixed_compound|root_wire_compound|edge_compound|vertex_compound' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep.rs`, `(cd rust/lean_occt && cargo test)`, `git diff --check`.

## M33. Rust-Owned Public Root Edge Endpoint Queries

Outcome: `Context::edge_endpoints()` and `ported_edge_endpoints()` for supported root line, circle, and ellipse edges stop using direct `edge_endpoints_occt()` as the public Rust-owned endpoint answer; public endpoints are derived from loaded Rust topology endpoint vertices.

Status: complete on 2026-04-26. `ported_edge_endpoints()` now rejects unsupported root edge kinds before loading topology, then calls `load_ported_topology()` and returns endpoint positions from the loaded topology edge's start/end vertex indices. The direct `edge_endpoints_occt()` call was removed from the public Rust endpoint path and isolated inside `root_edge_topology_bootstrap_seed()`, which is used only while constructing root-edge topology.

Definition of done: supported root line/circle/ellipse public endpoint queries load Rust topology and return the single edge's start/end vertex positions; the raw endpoint seed remains private to root-edge topology bootstrap or unsupported/oracle paths; unsupported root edge kinds still return `None` from the Rust path instead of forcing topology; and tests/source guards prove `ported_edge_endpoints()` no longer calls `edge_endpoints_occt()` directly.

Bounded tasks: complete. The public endpoint query no longer calls the bootstrap seed; unsupported root edge kinds still return `Ok(None)` before generic topology loading; supported and non-root edge queries derive endpoints from `LoadedPortedTopology`; root-edge topology bootstrap owns the remaining raw endpoint read; and the new workflow test checks public/ported/OCCT endpoint parity against loaded topology vertices for line, circle, and ellipse root edges.

Verification: `(cd rust/lean_occt && cargo fmt)`, `cmake --build build --target LeanOcctCAPI`, `(cd rust/lean_occt && cargo check)`, `(cd rust/lean_occt && cargo test --test ported_geometry_workflows public_root_edge_endpoints_are_topology_backed -- --nocapture)`, `(cd rust/lean_occt && cargo test --test ported_geometry_workflows root_edge_endpoints_and_topology_use_ported_seed -- --nocapture)`, `! perl -0ne 'print $1 if /(pub\(super\) fn ported_edge_endpoints[\s\S]*?)\npub\(super\) fn ported_subshape_count/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs | rg -n 'edge_endpoints_occt\('`, `perl -0ne 'print $1 if /(pub\(super\) fn ported_edge_endpoints[\s\S]*?)\npub\(super\) fn ported_subshape_count/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs | rg -n 'load_ported_topology|optional_vertex_position|RootEdgeEndpointTopologySupport'`, `rg -n 'root_edge_topology_bootstrap_seed|root_edge_topology_bootstrap_length|edge_endpoints_occt\(' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs`, `(cd rust/lean_occt && cargo test)`, `git diff --check`.

## M34. Rust-Owned Root Edge Topology Bootstrap Endpoint Seed

Outcome: root line/circle/ellipse edge topology construction stops using direct `edge_endpoints_occt()` as its bootstrap endpoint source; topology bootstrap endpoint positions come from root edge vertex handles and Rust topology/vertex seed data instead.

Status: complete on 2026-04-26. `root_edge_topology_bootstrap_seed()` now returns a `RootEdgeVertexSeed` built from `root_edge_vertex_shape_occt(shape, 0/1)` handles and public `Context::vertex_point()` results, so endpoint positions are resolved through the Rust-owned root vertex topology path instead of the direct edge endpoint helper. Closed root edges whose endpoint handles are the same still build a single topology vertex with both edge endpoints pointing at index `0`. `public_root_edge_endpoints_are_topology_backed` now also asserts public root-edge vertex handles resolve to the loaded topology vertex positions.

Definition of done: `load_root_edge_topology_snapshot()` can build supported root edge topology without calling `edge_endpoints_occt()`; the bootstrap uses the existing root edge vertex handles and Rust-owned vertex/topology seed path for endpoint positions; public endpoint/topology tests remain green for line/circle/ellipse roots; and source guards show no `edge_endpoints_occt()` calls remain in `brep/topology.rs` or `brep/shape_queries.rs`.

Bounded tasks: complete. The root-edge bootstrap seed now collects endpoint vertex handles before endpoint positions, derives those positions through the public vertex topology route, carries the resulting positions as bootstrap `EdgeEndpoints` for line-length calculation and topology edge construction, preserves same-handle closed-edge topology, keeps explicit `Context::edge_endpoints_occt()` available as an oracle API, and strengthens source guards around `topology.rs` and `shape_queries.rs`.

Verification: `(cd rust/lean_occt && cargo fmt)`, `! rg -n 'edge_endpoints_occt\(' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs`, `rg -n 'root_edge_topology_bootstrap_seed|root_edge_vertices_from_ported_seed|root_edge_vertex_shape_occt|vertex_point\(' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs`, `cmake --build build --target LeanOcctCAPI`, `(cd rust/lean_occt && cargo check)`, `(cd rust/lean_occt && cargo test --test ported_geometry_workflows public_root_edge_endpoints_are_topology_backed -- --nocapture)`, `(cd rust/lean_occt && cargo test --test ported_geometry_workflows root_edge_endpoints_and_topology_use_ported_seed -- --nocapture)`, `(cd rust/lean_occt && cargo test)`, `git diff --check`.

## M35. Rust-Owned Public Root Edge Support Classification

Outcome: public root edge endpoint queries stop making their own direct `edge_geometry_occt()` support-classification read before loading topology; supported and unsupported root edge classification is owned by the root topology loader, and unsupported root edge kinds do not fall through to the generic raw root inventory sweep.

Status: active, not started. After M34, endpoint data is Rust/topology-backed, but `shape_queries.rs::root_edge_endpoint_topology_support()` still calls `context.edge_geometry_occt(shape)?` just to decide whether a root edge is a supported line/circle/ellipse before `ported_edge_endpoints()` loads topology. The root topology loader also returns `None` for unsupported root edge kinds, which currently allows the generic raw root inventory loader to rebuild unsupported root edge topology through raw public fallbacks.

Definition of done: `ported_edge_endpoints()` has no direct `edge_geometry_occt()` or `edge_endpoints_occt()` calls in its support path; root-edge support classification is reported by the topology loader or a shared topology-owned classifier; unsupported root edge kinds return `Ok(None)` from ported topology/endpoint paths without entering the generic raw root inventory loader; and line/circle/ellipse root endpoint/topology regressions stay green.

Bounded tasks: remove `RootEdgeEndpointTopologySupport` from `shape_queries.rs`; teach `load_root_topology_snapshot()` or its root-edge branch to distinguish not-root-edge, supported-root-edge, and unsupported-root-edge without falling through to the generic loader for unsupported root edges; keep `edge_geometry_occt()` isolated to the topology-owned root-edge bootstrap until the next geometry-bootstrap milestone; and add source guards proving `shape_queries.rs` no longer calls direct edge geometry or endpoint helpers.

Verification: `(cd rust/lean_occt && cargo fmt)`, `cmake --build build --target LeanOcctCAPI`, `(cd rust/lean_occt && cargo check)`, `(cd rust/lean_occt && cargo test --test ported_geometry_workflows public_root_edge_endpoints_are_topology_backed -- --nocapture)`, `(cd rust/lean_occt && cargo test --test ported_geometry_workflows root_edge_endpoints_and_topology_use_ported_seed -- --nocapture)`, `! perl -0ne 'print $1 if /(pub\(super\) fn ported_edge_endpoints[\s\S]*?)\npub\(super\) fn ported_subshape_count/' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs | rg -n 'edge_geometry_occt\(|edge_endpoints_occt\(|RootEdgeEndpointTopologySupport'`, `rg -n 'RootEdgeTopologyInventory|UnsupportedRootEdge|load_root_edge_topology_snapshot' rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/topology.rs rust/lean_occt/src/occt_port/ModelingData/TKBRep/BRepTools/brep/shape_queries.rs`, `(cd rust/lean_occt && cargo test)`, `git diff --check`.
